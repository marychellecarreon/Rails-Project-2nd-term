<%= form_for(@buy) do |f| %>
  <% if @buy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@buy.errors.count, "error") %> prohibited this buy from being saved:</h2>

      <ul>
      <% @buy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Purchased_Date %><br>
    <p><span class="glyphicon glyphicon-calendar"><%= f.date_select :purchased_date %></p>
  </div>
 
  <div class ="field">
      <%= f.hidden_field :price, value: @milk.price, id: 'buy_price' %>
  </div>

   <div class ="field">
      <%= f.hidden_field :totalprice, id: 'buy_totalprice' %>
  </div>


  <div class="field">
        <%= f.hidden_field :milk_id, value: @milk.id %>
    </div>
    
    
<div class="btn btn-default">
    <%= f.submit %>
  </div>
<% end %>
